#proxy_pass http://0.0.0.0:5000/airbnb-onepage;
        #}
        location /airbnb-onepage/ {
                include proxy_params;
                proxy_pass http://0.0.0.0:5000;
        }

        location /hbnb_static/ {
                alias /data/web_static/current/;
        }

        location ~ ^/airbnb-dynamic/number_odd_or_even/(\d+)$ {
                include proxy_params;
                proxy_pass http://0.0.0.0:5001/number_odd_or_even/$1;
        }
        
        location /api/ {
                include proxy_params;
                proxy_pass http://0.0.0.0:5002;
        }

        # Self signed certs generated by the ssl-cert package
        # Don't use them in a production server!
        #
        # include snippets/snakeoil.conf;

        root /var/www/html;

        # Add index.php to the list if you are using PHP
        index index.html index.htm index.nginx-debian.html;

        add_header X-Served-By 108343-web-01;
        server_name _;
        error_page 404 /404.html; location = /404.html {root /var/www/error/;internal; }

        if ($request_filename ~ redirect_me){
                rewrite ^ https://https://twitter.com/search?q=%22David%20Ndii%22&src=trend_click&vertical=trends permanent;
